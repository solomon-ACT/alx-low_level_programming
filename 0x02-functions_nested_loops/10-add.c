#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @one: The First Number
 * @two: The Scond Number
 * Return: The Sum Of one + two
 */
int add(int one, int two)
{
return (one + two);
}
